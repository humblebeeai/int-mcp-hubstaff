version: '3.8'

services:
  hubstaff-mcp:
    build: .
    container_name: hubstaff-mcp-server
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./tokens.json:/app/tokens.json
    restart: unless-stopped
